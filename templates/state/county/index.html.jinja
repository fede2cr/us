{% extends "base.html.jinja" %}
{% block title %}{{ county.name }}{% endblock %}
{% block content %}
    <h1>{{ county.name }} necesita tu voz.</h1>
    <ul id="dates">
    {% for date in dates %}
      <li>{{ date.date }} - {{ date.name }}</li>
    {% endfor %}
    </ul>
    Agregue todas las fechas a tu calendario, haciendo click <a href="webcal://electioncal.us/{{language}}/{{state.lower_name}}/{{county.lower_name}}/voter.ics">aquí</a> (
    {% for alternative in alternatives %}<a href="https://electioncal.us/{{language}}/{{state.lower_name}}/{{county.lower_name}}/voter.{{alternative.extension}}">{{alternative.name}}</a>{% if not loop.last %}, {% endif %}
    {% endfor %}).
{% endblock %}
